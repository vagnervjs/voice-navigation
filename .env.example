# =============================================================================
# Voice Navigation - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values
# cp .env.example .env

# =============================================================================
# Google Maps Configuration
# =============================================================================

# Your Google Maps API Key - Get it from: https://console.cloud.google.com/
# Required APIs: Maps JavaScript API, Geocoding API
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# Google Maps Geocoding API endpoint (usually no need to change)
VITE_GEOCODING_BASE_URL=https://maps.googleapis.com/maps/api/geocode/json

# Google Maps API version (weekly, quarterly, or specific version)
VITE_GOOGLE_MAPS_VERSION=weekly

# =============================================================================
# Voice Recognition Settings
# =============================================================================

# Voice recognition confidence threshold (0.0 - 1.0)
# Lower values = more sensitive, Higher values = more accurate
VITE_VOICE_CONFIDENCE_THRESHOLD=0.6

# Voice recognition language code
# Supported: en-US, en-GB, es-ES, fr-FR, de-DE, it-IT, pt-BR, etc.
VITE_VOICE_LANGUAGE=en-US

# =============================================================================
# User Interface Configuration
# =============================================================================

# Default map zoom level (1-100, higher = more zoomed in)
VITE_MAPS_DEFAULT_ZOOM=100

# Geolocation timeout in milliseconds (10000 = 10 seconds)
VITE_GEOLOCATION_TIMEOUT=10000

# =============================================================================
# Development Notes
# =============================================================================
# 
# 1. Get Google Maps API Key:
#    - Go to https://console.cloud.google.com/
#    - Create a new project or select existing one
#    - Enable "Maps JavaScript API" and "Geocoding API"
#    - Create credentials (API Key)
#    - Optionally restrict the key to your domain
#
# 2. Voice Recognition:
#    - Works best in Chrome
#    - Requires HTTPS or localhost
#    - May need microphone permissions
#
# 3. All VITE_ prefixed variables are accessible in the frontend
#    Other variables are only available in the build process
#
